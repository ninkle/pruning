"""Make `pruning` installable."""
from setuptools import setup

setup(
    name = "pruning",
    version = "1.0",
    author = "Nicole Fitzgerald",
    description = "Scripts and utilities to implement a pruning mechanism for neural networks.",
    keywords = "pruning",
    packages=["pruning"],
)
